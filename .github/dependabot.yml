# Keeps the GitHub Actions pins in .github/workflows/ current. No other ecosystems:
# the repo holds only the publish workflow and its scripts, no package manifests.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    labels: ["dependencies"]
    commit-message:
      prefix: "chore(deps)"
